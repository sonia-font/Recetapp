export default class Filtro {
    constructor(keyWord, maxIngredients, maxTime, difficulty){
        this.keyWord = keyWord,
        this.maxIngredients = maxIngredients,
        this.maxTime = maxTime,
        this.difficulty = difficulty
    }
}